<%= form_for ([@post, @post.comments.build]) do |f| %>
  <p><%= f.label :Name %></p><br>
  <p><%= f.text_field :name %></p>
  
  <p><%= f.label :Body %></p><br>
  <p><%= f.text_area :body %></p><br>
  <br>
  
  <p><%= f.submit %></p>

<% end %>